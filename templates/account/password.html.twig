{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe : Cabinet de Bien-Être - Sandrine Picod{% endblock %}

{% block content %}

    <h1>Modifier mon mot de passe</h1>
    Bienvenue {{ app.user.firstname }} dans votre espace
    <hr>
    <a href="{{ path('account') }}">Retourner à mon compte.</a> <br/>
    <br>
    {% if notification %}
        <div class="alert alert-warning">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}

    <hr>

{% endblock %}