<form method="post" action="{{ path('admin_pack_delete', {'id': pack.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer ce forfait?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pack.id) }}">
    <button class="btn btn-danger">SUPPRIMER</button>
</form>
