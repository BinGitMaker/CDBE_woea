<div class="row mt-4">
    <div class="d-flex justify-content-center">
        <div class="button-group">
            <div class="d-flex flex-row">
                {% for prodCategory in prodCategories %}

                    <a href="{{ path('product_by_category', {'slug' : prodCategory.slug}) }}" 
                    class="btn btn-cat">{{ prodCategory.name }}</a>
                    
                {% endfor %}
            </div>
        </div>
    </div>
</div>